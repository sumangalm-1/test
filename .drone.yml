---
kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: ppc64le

steps:
- name: clone
  commands:
  - git clone https://github.com/sumangalm-1/test.git .


trigger:
  branch:
  - main
